server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dan-Elioth/ExamenMicro.git
          search-paths: config-data
          username: dancito
          password: 321
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
